{
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "fast-check": "^3.15.0",
    "jsdom": "^23.0.0",
    "glob": "^10.3.10"
  },
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "server": "node backend/server.js",
    "server:dev": "node --watch backend/server.js"
  }
}
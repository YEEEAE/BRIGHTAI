document.addEventListener("DOMContentLoaded",()=>{const faqItems=document.querySelectorAll(".faq-item");faqItems.length>0&&faqItems.forEach(item=>{item.querySelector(".faq-question").addEventListener("click",()=>{const isOpen=item.classList.contains("open");faqItems.forEach(otherItem=>{otherItem!==item&&otherItem.classList.remove("open")}),item.classList.toggle("open")})}),document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener("click",function(e){e.preventDefault();const targetId=this.getAttribute("href");if(targetId==="#")return;const targetElement=document.querySelector(targetId);targetElement&&targetElement.scrollIntoView({behavior:"smooth"})})});const observerOptions={threshold:.1,rootMargin:"0px 0px -50px 0px"},observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{entry.isIntersecting&&(entry.target.classList.add("animate-fade-in-up"),observer.unobserve(entry.target))})},observerOptions);document.querySelectorAll(".glass-card").forEach(card=>{observer.observe(card)});const mobileToggle=document.querySelector(".mobile-toggle"),mobileMenu=document.querySelector(".mobile-menu-drawer"),backdrop=document.querySelector(".backdrop-overlay");mobileToggle&&mobileMenu&&(mobileToggle.addEventListener("click",()=>{mobileMenu.classList.toggle("active"),backdrop.classList.toggle("active"),document.body.classList.toggle("overflow-hidden");const icon=mobileToggle.querySelector("iconify-icon");if(icon){const iconName=mobileMenu.classList.contains("active")?"lucide:x":"lucide:menu";icon.setAttribute("icon",iconName)}}),backdrop&&backdrop.addEventListener("click",()=>{mobileMenu.classList.remove("active"),backdrop.classList.remove("active"),document.body.classList.remove("overflow-hidden")}))});
